extends layout

block content
  h1 Here the Articles:
  if articles
    each item in articles
      div
        h2
          a(href='/article/' + item._id) "#{item.title}" by #{item.author}
        p #{item.body}
        p Published at #{item.publishedAt}
  else
    h2 We are sorry, but we can not find articles
